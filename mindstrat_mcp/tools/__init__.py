"""Tool implementations, grouped by MindStrat app section.

Every tool is a plain async function taking the BackendClient as its first
argument, so the tool layer stays independent of the MCP transport.
"""
